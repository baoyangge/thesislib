# Copy to .env

# iron-session: must be >= 32 chars, use a strong random string
SESSION_PASSWORD="CHANGE_ME_TO_A_LONG_RANDOM_STRING_AT_LEAST_32_CHARS"

# SQLite persisted locally
DATABASE_URL="file:./dev.db"

# Bootstrap an admin on signup (optional but recommended)
ADMIN_EMAIL="woshibaoyangge@gmail.com"

# Upload limit in MB
MAX_UPLOAD_MB="20"
